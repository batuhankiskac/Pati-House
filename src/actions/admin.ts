/**
 * Deprecated file.
 * All legacy server actions (addCat, updateCat, deleteCat, updateRequestStatus)
 * have been removed after migration to REST API routes and client hooks (useCats, useRequests).
 *
 * Kept intentionally empty (no 'use server') to eliminate server action registration.
 */
